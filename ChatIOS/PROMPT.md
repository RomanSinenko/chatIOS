# Проект
Нативный iOS-клиент для чат-приложения.

Структура проекта:
- backend: `/Users/romansinenko/Desktop/prog/Chat/backend`
- ios: `/Users/romansinenko/Desktop/prog/Chat/ios`

Проект развивается как MVP чат-приложения с прицелом на дальнейший рост в полноценный мессенджер.

---

# Цель iOS-части
Собрать нативный iOS-клиент на `Swift` / `SwiftUI`, который работает с готовым backend API.

MVP-экраны:
- Старт / Вход
- Список чатов
- Чат
- Поиск
- Новый чат / Контакты

---

# Текущий статус iOS
Сейчас `ChatIOS` — это новый Xcode SwiftUI проект с базовым app skeleton, статическим chat-flow и первым подключением к backend.

Что есть сейчас:
- базовый `ChatIOSApp.swift`
- `ContentView.swift` как корневой экран с `NavigationStack`
- `StartView.swift` для стартового входа
- `ChatsListView.swift` для экрана списка чатов
- `ContactsView.swift` для экрана контактов / выбора пользователя перед открытием чата
- `ChatView.swift` для экрана конкретного чата
- `Contact.swift` как временная модель контакта
- `ChatMessage.swift` как временная модель сообщения
- `ChatUser.swift` как модель пользователя из backend
- `APIClient.swift` как простой HTTP client для backend
- стандартные test / ui test файлы

Уже подключено:
- старый временный вход через `POST /users/{user_name}` со стартового экрана
- успешное создание пользователя через backend по старому username-flow
- переход к списку чатов после успешного ответа
- базовая обработка ошибок `400`, `409` и network/server unavailable
- `GET /users/{user_id}/chats` для загрузки списка чатов пользователя

Пока не реализованы:
- новый dev-login через телефон
- поиск пользователей через backend
- создание private chat из iOS
- загрузка истории сообщений
- WebSocket

---

# Что важно про backend
Backend уже существует и умеет базовый chat MVP.

Основные готовые HTTP endpoints:
- `POST /auth/dev-login?phone=...` — временный вход/регистрация по телефону без SMS
- `POST /users/{username}?display_name=...` — dev-создание пользователя с custom username
- `GET /users/search?query=...` — поиск пользователей только по `username`
- `POST /private-chats/{user_id}/{peer_user_id}` — найти или создать private chat
- `GET /users/{user_id}/chats` — список чатов пользователя
- `GET /chats/{chat_id}?user_id=...` — мета-информация о чате
- `GET /chats/{chat_id}/messages?user_id=...` — история сообщений с membership check

WebSocket:
- `WS /ws/{user_id}`

Формат исходящего сообщения от клиента:

```json
{
  "chat_id": 1,
  "to_user_id": 2,
  "text": "hello"
}
```

Форматы входящих событий с сервера:
- `message`
- `system`
- `error`
- `message_ack`

Что уже поддерживается на backend:
- временный dev-login по телефону без SMS
- `username` как публичный уникальный идентификатор
- `display_name` как неуникальное отображаемое имя, может быть `null`
- отдельная таблица телефонов пользователя
- единая backend-логика отображения собеседника:
  - сейчас `display_name`, если есть, иначе `username`
  - телефон другим пользователям не показывается
- private chat flow
- self-chat
- список чатов
- история сообщений
- WebSocket realtime
- `message_ack`
- membership checks

---

# Требуется реализовать на backend
Этот раздел фиксирует backend-задачи, которые всплывают во время разработки iOS, чтобы не потерять их.

Нужно:
- позже заменить dev-login по телефону на полноценную авторизацию:
  - SMS/OTP
  - сессии/токены
  - устройства пользователя
- позже добавить возможность менять дефолтный `username`, если новый свободен
- позже добавить Settings для `display_name`, `username` и logout
- позже добавить privacy-настройки:
  - `show_phone_number`
  - `can_find_by_phone`
- добавить стабильные error codes в HTTP-ошибки, чтобы iOS не завязывался на текст `detail`
  - пример: `username_taken`, `username_too_short`, `username_invalid_characters`
- позже пересмотреть endpoint создания/получения private chat под мобильный flow:
  - выбрать контакт
  - получить или создать private chat
  - открыть историю
  - подключить WebSocket
- перед реальным публичным запуском продумать auth/legal flow:
  - телефон
  - SMS/OTP
  - хранение устройств
  - privacy policy / персональные данные

---

# Текущий фокус
Продолжать разработку именно iOS-клиента.

Важно:
- работать на уровне `/Users/romansinenko/Desktop/prog/Chat`
- backend и ios лежат в отдельных директориях
- без явной команды не менять backend

---

# Что сделано в большом шаге `ios-app-skeleton`
Ветка: `ios-app-skeleton`

Сделано:
- создана базовая структура iOS-проекта по папкам:
  - `App`
  - `Views`
  - `Models`
  - `Services`
  - `ViewModels`
- `ChatIOSApp.swift` перенесён в `App`
- `ContentView.swift` перенесён в `Views`
- создан `StartView`
- добавлен ввод имени пользователя через `TextField`
- добавлена кнопка `Продолжить`
- `StartView` передаёт введённое имя наружу через `onContinue`
- `ContentView` хранит текущего пользователя в `@State`
- добавлен переход из стартового экрана к экрану чатов
- создан `ChatsListView`
- добавлена кнопка `Выйти`
- `ChatsListView` передаёт событие выхода наружу через `onLogout`
- `ContentView` очищает текущего пользователя при выходе и возвращает на стартовый экран
- добавлен `NavigationStack`
- экран чатов переведён на `List`
- добавлена кнопка с иконкой карандаша в верхнюю панель
- создан `ContactsView`
- кнопка с карандашом открывает `ContactsView` через `NavigationLink`

Текущий flow:

```text
ContentView
  если пользователя нет
    -> StartView
  если пользователь есть
    -> ChatsListView
        кнопка карандаша
          -> ContactsView
```

Пока это локальный UI-skeleton без backend.

---

# Что сделано в большом шаге `ios-static-chat-flow`
Ветка: `ios-static-chat-flow`

Сделано:
- создана модель `Contact`
- создана модель `ChatMessage`
- в `ContactsView` добавлен временный список контактов
- контакты показываются через `List` и `ForEach`
- каждая строка контакта открывает `ChatView` через `NavigationLink`
- создан `ChatView`
- `ChatView` получает выбранный `Contact`
- добавлены временные сообщения для проверки UI без backend
- сообщения отображаются списком
- свои сообщения выравниваются вправо
- сообщения собеседника выравниваются влево
- добавлены простые message bubbles
- добавлена нижняя панель ввода сообщения
- кнопка отправки пока печатает сообщение в консоль и очищает поле ввода
- добавлены учебные комментарии к ключевым новым местам

Текущий flow:

```text
Список чатов
  кнопка карандаша
    -> ContactsView
        выбрать контакт
          -> ChatView(contact)
```

Пока это локальный статический flow без backend.

---

# Что сделано в большом шаге `ios-auth-api-client`
Ветка: `ios-auth-api-client`

Важно:
- этот раздел описывает уже выполненный исторический шаг
- после backend-шага `backend-mobile-contract-polish` старый `user_name`-flow больше не является целевым
- дальше iOS нужно переводить на `POST /auth/dev-login?phone=...`

Сделано:
- создана модель `ChatUser`
- добавлен `Decodable` для разбора JSON от backend
- добавлен `CodingKeys`, чтобы связать старое backend поле `user_name` со Swift свойством `userName`
- создан `APIClient`
- добавлен базовый `baseURL` для локального backend `http://127.0.0.1:8000`
- реализован `createUser(userName:)`
- исправлена сборка URL через `appendingPathComponent`, чтобы не было двойного percent-encoding кириллицы
- `ContentView` теперь хранит `currentUser: ChatUser?`, а не только имя строкой
- `StartView` принимает `isLoading` и `errorMessage`
- при нажатии `Продолжить` iOS отправляет `POST /users/{user_name}`
- при успешном ответе backend пользователь сохраняется в `currentUser`
- после успешного создания пользователя открывается `ChatsListView`
- во время запроса кнопка показывает `Загрузка...`
- при ошибке показывается сообщение на стартовом экране
- для `400` показывается ошибка невалидного имени
- для `409` показывается ошибка занятого имени
- при недоступном backend показывается ошибка подключения

Текущий auth flow:

```text
StartView
  ввод имени
    -> APIClient.createUser(userName)
      -> POST /users/{user_name}
        -> ChatUser(id, userName)
          -> ContentView.currentUser
            -> ChatsListView
```

Этот auth-flow больше не целевой. Следующий шаг переводит iOS на временный вход по телефону без SMS.

---

# Что сделано в большом шаге `ios-user-chats-api-client`
Ветка: `ios-user-chats-api-client`

Сделано:
- создана модель `LastMessage`
- создана модель `ChatSummary`
- добавлен `APIClient.getUserChats(userID:)`
- `ChatsListView` теперь принимает `userID`
- `ContentView` передаёт `currentUser.id` в `ChatsListView`
- `ChatsListView` хранит состояние загрузки чатов:
  - `chats`
  - `isLoadingChats`
  - `chatsErrorMessage`
- при открытии `ChatsListView` вызывается загрузка чатов через `.onAppear`
- список чатов загружается через `GET /users/{user_id}/chats`
- UI показывает состояния:
  - загрузка
  - ошибка
  - пустой список
  - список реальных чатов
- для каждого чата показывается `displayName`
- если есть `lastMessage`, показывается текст последнего сообщения
- если последнего сообщения нет, показывается пустое состояние для строки

Пока ограничение:
- этот раздел был актуален до backend-шага `backend-mobile-contract-polish`
- сейчас backend уже умеет `POST /auth/dev-login?phone=...`
- iOS ещё не переведён на этот endpoint, поэтому это и есть следующий большой шаг

---

# Следующий большой шаг
Следующий большой шаг: `ios-dev-login-and-private-chat-flow`

Цель:
- перевести iOS со старого username-входа на новый backend dev-login по телефону
- подготовить flow выбора пользователя и открытия private chat
- начать открывать реальный чат с backend-данными, а не только статический экран

План внутри большого шага:
- обновить `ChatUser` под новый backend response:
  - `id`
  - `username`
  - `display_name`, nullable
  - `is_username_custom`
  - `phone_verified`
- обновить `APIClient`:
  - добавить `devLogin(phone:)`
  - использовать `POST /auth/dev-login?phone=...`
- обновить `StartView`:
  - ввод только телефона
  - без SMS на этом этапе
- обновить `ContentView`:
  - вызывать новый `devLogin`
  - сохранять текущего пользователя из нового ответа
- обновить поиск/контакты:
  - искать пользователей через `GET /users/search?query=...`
  - искать только по `username`, не по `display_name`
- добавить создание/получение private chat:
  - `POST /private-chats/{user_id}/{peer_user_id}`
- открыть `ChatView` с backend-данными:
  - `chat_id`
  - `current_user_id`
  - `peer_user_id`
  - имя собеседника
- загрузить историю:
  - `GET /chats/{chat_id}/messages?user_id=...`
- WebSocket оставить следующим отдельным большим шагом, если объем станет слишком большим.

Пока не делаем:
- SMS/OTP
- JWT/session auth
- поиск по телефону
- отображение телефона другим пользователям
- группы
- звонки

---

# Правила работы
- двигаться маленькими шагами
- сначала читать контекст, потом предлагать или делать следующий шаг
- не вносить изменения в код без прямой команды
- если задача касается только iOS, не уходить в backend-рефакторинг
- перед правками коротко фиксировать, что именно будет изменено
- держать ответы короткими, по делу, без перегруза

---

# Правила общения
- следовать командам пользователя и не торопиться
- не делать лишних действий сверх запроса
- если что-то уже прочитано или сделано, сообщать это прямо и точно
- если есть риск затронуть не ту часть проекта, сначала уточнить или остановиться
- если просят сначала прочитать контекст, сначала читать, а не предлагать реализацию

---

# Важно про обучение
Пользователь раньше не писал на `Swift`.

Поэтому:
- все новые методы, функции, конструкции и решения нужно объяснять
- архитектурные решения тоже нужно кратко раскладывать по смыслу: что это, зачем это и как работает
- по возможности объяснять простыми словами
- не перегружать теорией, но не оставлять новые вещи без пояснения

---

# Формат нашей работы
Работаем так:
- сначала определяем крупный шаг, который нужно сделать
- под этот шаг создаём отдельную git-ветку
- дальше двигаемся короткими шагами
- на каждом коротком шаге AI пишет:
  - в какой файл нужно внести код
  - какую функцию, структуру или часть кода нужно добавить
  - сам код для вставки
- после кода AI отдельно объясняет:
  - как это работает
  - зачем это нужно
  - какие новые методы, функции или конструкции там использованы
- пользователь переписывает код вручную сам
- после того как пользователь переписал код и всё прочитал, он присылает `"+"`
- сообщение `"+"` означает: можно двигаться дальше к следующему короткому шагу

Важно:
- AI не должен торопиться и перескакивать через шаги
- AI не должен выдавать сразу большой объём реализации на много шагов вперёд
- один ответ = один понятный короткий шаг в рамках текущего большого шага

---

# Завершение большого шага
Когда большой шаг завершён:
- обновляем `PROMPT.md` по аналогии с backend-подходом
- фиксируем, что именно было сделано в рамках шага
- сразу определяем следующий большой шаг
- после этого делаем `git push`
- затем продолжаем работу уже от следующего большого шага

Это нужно, чтобы:
- сохранять живой контекст проекта
- не терять прогресс между этапами
- всегда понимать текущее состояние iOS-части
