# Проект
Нативный iOS-клиент для чат-приложения.

Структура проекта:
- backend: `/Users/romansinenko/Desktop/prog/Chat/backend`
- ios: `/Users/romansinenko/Desktop/prog/Chat/ios`

Проект развивается как MVP чат-приложения с прицелом на дальнейший рост в полноценный мессенджер.

---

# Цель iOS-части
Собрать нативный iOS-клиент на `Swift` / `SwiftUI`, который работает с готовым backend API.

MVP-экраны:
- Старт / Вход
- Список чатов
- Чат
- Поиск
- Новый чат / Контакты

---

# Текущий статус iOS
Сейчас `ChatIOS` — это новый Xcode SwiftUI проект с базовым app skeleton.

Что есть сейчас:
- базовый `ChatIOSApp.swift`
- `ContentView.swift` как корневой экран с `NavigationStack`
- `StartView.swift` для стартового входа по имени пользователя
- `ChatsListView.swift` для экрана списка чатов
- `ContactsView.swift` для экрана контактов / выбора пользователя перед открытием чата
- стандартные test / ui test файлы

Пока сетевой слой, модели, реальный список чатов, поиск пользователей и экран конкретного чата не реализованы.

---

# Что важно про backend
Backend уже существует и умеет базовый chat MVP.

Основные готовые HTTP endpoints:
- `POST /users/{user_name}` — создать пользователя
- `GET /users/search?query=...` — поиск пользователей
- `POST /private-chats/{user_id}/{peer_user_id}` — найти или создать private chat
- `GET /users/{user_id}/chats` — список чатов пользователя
- `GET /chats/{chat_id}?user_id=...` — мета-информация о чате
- `GET /chats/{chat_id}/messages` — история сообщений

WebSocket:
- `WS /ws/{user_id}`

Формат исходящего сообщения от клиента:

```json
{
  "chat_id": 1,
  "to_user_id": 2,
  "text": "hello"
}
```

Форматы входящих событий с сервера:
- `message`
- `system`
- `error`
- `message_ack`

Что уже поддерживается на backend:
- private chat flow
- self-chat
- список чатов
- история сообщений
- WebSocket realtime
- `message_ack`
- membership checks

---

# Текущий фокус
Продолжать разработку именно iOS-клиента.

Важно:
- работать на уровне `/Users/romansinenko/Desktop/prog/Chat`
- backend и ios лежат в отдельных директориях
- без явной команды не менять backend

---

# Что сделано в большом шаге `ios-app-skeleton`
Ветка: `ios-app-skeleton`

Сделано:
- создана базовая структура iOS-проекта по папкам:
  - `App`
  - `Views`
  - `Models`
  - `Services`
  - `ViewModels`
- `ChatIOSApp.swift` перенесён в `App`
- `ContentView.swift` перенесён в `Views`
- создан `StartView`
- добавлен ввод имени пользователя через `TextField`
- добавлена кнопка `Продолжить`
- `StartView` передаёт введённое имя наружу через `onContinue`
- `ContentView` хранит текущего пользователя в `@State`
- добавлен переход из стартового экрана к экрану чатов
- создан `ChatsListView`
- добавлена кнопка `Выйти`
- `ChatsListView` передаёт событие выхода наружу через `onLogout`
- `ContentView` очищает текущего пользователя при выходе и возвращает на стартовый экран
- добавлен `NavigationStack`
- экран чатов переведён на `List`
- добавлена кнопка с иконкой карандаша в верхнюю панель
- создан `ContactsView`
- кнопка с карандашом открывает `ContactsView` через `NavigationLink`

Текущий flow:

```text
ContentView
  если пользователя нет
    -> StartView
  если пользователь есть
    -> ChatsListView
        кнопка карандаша
          -> ContactsView
```

Пока это локальный UI-skeleton без backend.

---

# Следующий большой шаг
Следующий большой шаг: `ios-static-chat-flow`

Цель:
- добавить экран конкретного чата `ChatView`
- добавить временные статические контакты в `ContactsView`
- сделать выбор контакта
- после выбора контакта открывать `ChatView`
- пока без backend и WebSocket

Ожидаемый flow:

```text
Список чатов
  -> Контакты
      -> выбрать контакт
          -> Чат с выбранным контактом
```

---

# Правила работы
- двигаться маленькими шагами
- сначала читать контекст, потом предлагать или делать следующий шаг
- не вносить изменения в код без прямой команды
- если задача касается только iOS, не уходить в backend-рефакторинг
- перед правками коротко фиксировать, что именно будет изменено
- держать ответы короткими, по делу, без перегруза

---

# Правила общения
- следовать командам пользователя и не торопиться
- не делать лишних действий сверх запроса
- если что-то уже прочитано или сделано, сообщать это прямо и точно
- если есть риск затронуть не ту часть проекта, сначала уточнить или остановиться
- если просят сначала прочитать контекст, сначала читать, а не предлагать реализацию

---

# Важно про обучение
Пользователь раньше не писал на `Swift`.

Поэтому:
- все новые методы, функции, конструкции и решения нужно объяснять
- архитектурные решения тоже нужно кратко раскладывать по смыслу: что это, зачем это и как работает
- по возможности объяснять простыми словами
- при необходимости можно проводить аналогии с `Python`, чтобы быстрее было понятно
- не перегружать теорией, но не оставлять новые вещи без пояснения

---

# Формат нашей работы
Работаем так:
- сначала определяем крупный шаг, который нужно сделать
- под этот шаг создаём отдельную git-ветку
- дальше двигаемся короткими шагами
- на каждом коротком шаге AI пишет:
  - в какой файл нужно внести код
  - какую функцию, структуру или часть кода нужно добавить
  - сам код для вставки
- после кода AI отдельно объясняет:
  - как это работает
  - зачем это нужно
  - какие новые методы, функции или конструкции там использованы
- пользователь переписывает код вручную сам
- после того как пользователь переписал код и всё прочитал, он присылает `"+"`
- сообщение `"+"` означает: можно двигаться дальше к следующему короткому шагу

Важно:
- AI не должен торопиться и перескакивать через шаги
- AI не должен выдавать сразу большой объём реализации на много шагов вперёд
- один ответ = один понятный короткий шаг в рамках текущего большого шага

---

# Завершение большого шага
Когда большой шаг завершён:
- обновляем `PROMPT.md` по аналогии с backend-подходом
- фиксируем, что именно было сделано в рамках шага
- сразу определяем следующий большой шаг
- после этого делаем `git push`
- затем продолжаем работу уже от следующего большого шага

Это нужно, чтобы:
- сохранять живой контекст проекта
- не терять прогресс между этапами
- всегда понимать текущее состояние iOS-части
